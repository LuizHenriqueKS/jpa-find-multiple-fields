spring:
    application:
        name: jpa-find-multiple-fields
    datasource:
        url: jdbc:h2:mem:testdb
        username: sa
        password:
    jpa:
        defer-datasource-initialization: true
    
server:
    port: 8181